<div
  x-show="appTickets.modalIsOpen"
  x-cloak
  class="fixed inset-0 z-50 overflow-y-auto"
  aria-labelledby="ticket-modal-title"
  role="dialog"
  aria-modal="true"
>
  <div class="flex items-center justify-center min-h-screen px-4 text-center">
    <div
      x-show="appTickets.modalIsOpen"
      x-transition:enter="transition ease-out duration-300 transform"
      x-transition:enter-start="opacity-0"
      x-transition:enter-end="opacity-100"
      x-transition:leave="transition ease-in duration-200 transform"
      x-transition:leave-start="opacity-100"
      x-transition:leave-end="opacity-0"
      @click="appTickets.modalClose()"
      class="fixed inset-0 transition-opacity bg-black/75"
      aria-hidden="true"
    ></div>

    <div
      x-show="appTickets.modalIsOpen"
      x-transition:enter="transition ease-out duration-300 transform"
      x-transition:enter-start="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
      x-transition:enter-end="opacity-100 translate-y-0 sm:scale-100"
      x-transition:leave="transition ease-in duration-200 transform"
      x-transition:leave-start="opacity-100 translate-y-0 sm:scale-100"
      x-transition:leave-end="opacity-0 translate-y-4 sm:translate-y-0 sm:scale-95"
      class="inline-block w-full max-w-lg p-6 my-8 overflow-hidden text-left align-middle transition-all transform border border-white/5 bg-background text-t-primary"
    >
      <div class="flex items-start justify-between gap-4 mb-4">
        <h3 id="ticket-modal-title" class="text-xl font-bold truncate">
          Create a Support Ticket
        </h3>

        <button @click="appTickets.modalClose()" class="text-t-primary focus:outline-none hover:text-accent">
          <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-7 text-t-primary/50 hover:text-accent-500">
            <path stroke-linecap="round" stroke-linejoin="round" d="M6 18 18 6M6 6l12 12"></path>
          </svg>
        </button>
      </div>

      <form @submit.prevent="appTickets.submitTicket()">
        <div class="mb-4">
          <label class="block text-t-primary/80 mb-1">
            Invoice
          </label>
          <input
            type="text"
            x-model="appTickets.invoiceId"
            readonly
            class="w-full py-2 px-4 text-t-primary/60 bg-white/5 border border-white/5 pointer-events-none"
          />
        </div>

        <div class="mb-4">
          <label class="block text-t-primary/80 mb-1">
            Subject
          </label>
          <input
            type="text"
            x-model="appTickets.subject"
            x-ref="appTickets.subjectInput"
            placeholder="Brief description of your issue"
            maxlength="255"
            class="w-full py-2 px-4 text-t-primary bg-white/5 border border-white/5 focus:border-accent-500 focus:ring-accent-500 focus:ring-2 focus:ring-opacity-50 focus:outline-none"
            required
          />
        </div>

        <div>
          <label class="block text-t-primary/80 mb-1">
            Message
          </label>
          <textarea
            x-model="appTickets.message"
            placeholder="Please describe your issue in detail..."
            maxlength="8000"
            rows="6"
            class="w-full py-2 px-4 text-t-primary bg-white/5 border border-white/5 focus:border-accent-500 focus:ring-accent-500 focus:ring-2 focus:ring-opacity-50 focus:outline-none resize-vertical"
            required
          ></textarea>
        </div>

        <p x-show="appTickets.error" class="text-red-500 text-sm mt-2" x-text="appTickets.error"></p>

        <button
          type="submit"
          class="w-full px-6 py-2 mt-4 flex-1 flex justify-center items-center gap-4 text-base text-t-accent font-semibold border border-accent-500 bg-accent-500 transition-colors ease-in-out duration-150 hover:enabled:bg-accent-500/50 hover:enabled:text-t-primary disabled:opacity-50 disabled:pointer-events-none"
          :disabled="appTickets.loading"
        >
          <span>Submit Ticket</span>
          <template x-if="!appTickets.loading">
            <i class="fa fa-arrow-right"></i>
          </template>
          <template x-if="appTickets.loading">
            <i class="fa fa-circle-notch animate-spin"></i>
          </template>
        </button>
      </form>
    </div>
  </div>
</div>